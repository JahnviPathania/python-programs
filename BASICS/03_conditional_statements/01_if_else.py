#if-else 
#condition in if is checked first if true the block of statement gets printed id it is false it goes to else and so on
age=int(input("enter your age:  "))
if(age>=18):
    print("you are eligible for voting")
else:
   print("cannot vote")    
#indentation is very important in python 
# Indentation means adding spaces or tabs at the beginning of a line to show that it's part of a block

